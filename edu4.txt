Fine Project. Fourth File

git init (initialized the git"

git remote add origin "Path"

git pull origin main

git add file name (Adding file to the index)

git commit -m "Adding first commit in local repository" (Commiting single file)

git add -A (Adding multiple files together in the index)

rm -f .git/index.lock (Try deleting index.lock file in your .git directory)

git commit -a -m "Adding 3 files" (Commiting multiple files together)

git config --global user.name "Ajitesh"

git config user.name "Ajitesh"

git config user.email "email id"

git status

git log

git branch firstbranch

git checkout firstbranch  (Switching of branch)

ls (Listing all files on firstbranch)

git checkout master (git checkout to master branch)

git merge firstbranch (We must checked out from the firstbranch before merging)

git commit -a -m "modifying edu5" (edu5 must be added to the index before it is Commiting)

cat edu5.txt (Display the content of edu5.txt. to effect the modification in edu5.txt on the other branch we need to merge 2 branches)

git rebase firstbranch
